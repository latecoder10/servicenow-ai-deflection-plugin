export interface AttachmentMetadata {
  id: string;
  attachmentSysId: string;
  fileName: string;
  mimeType: string;
  fileSize: number;
  tableName: string;
  recordSysId: string;
  downloadUrl: string;
  createdOn: string;
}
